# ===========================================
# MiroFish Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core framework =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM related =============
# OpenAI SDK (unified LLM calls via OpenAI format)
openai>=1.0.0

# ============= Local graph store =============
# NetworkX (graph structure)
networkx>=3.0
# numpy (cosine similarity computation)
numpy>=1.24.0

# ============= OASIS social media simulation =============
# OASIS social simulation framework
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= File processing =============
PyMuPDF>=1.24.0
# Encoding detection (supports non-UTF-8 encoded text files)
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= Utility libraries =============
# Environment variable loading
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0
